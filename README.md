# Campaigns-assignment

Please run- 
npm install ----- to add all the dependencies required for running the server in local

npm start ----------- to start the server on local machine


Routes

Routes are provided in routes.js file

typical route looks like ---- localhost:{port_number}/{end_point} ; ex: localhost:3000/api/v1/campaigns
